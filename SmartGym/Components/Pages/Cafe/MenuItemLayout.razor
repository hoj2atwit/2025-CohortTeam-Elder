@using Components.UIClasses.Cafe;
<button class="flex max-w-1/4 min-w-50 flex-col border border-black shadow-2xl" @onclick="OnClickCallback">
    <img class="aspect-square h-50" id="menuItemImage" src=@Assets[@item.ImageLocation] />
    <div class="p-4 text-gray-800">
        <h5 class="mb-2 text-lg font-semibold" >@item.Name</h5>
        <p class="text-base">@item.Description</p>
        <p class="text-base">@item.PriceString</p>
    </div>
</button>

@code {
    [Parameter]
    public MenuItemModel? item { get; set; }
    [Parameter]
    public EventCallback OnClickCallback { get; set; }

}
