@*@if(notifDTO.isRead)
    {   Different in color to mark as read
        <div class="w-full flex-col">
            <label>notifDTO.Title</label>
            <p> notifDTO.Content</p>
            <h3>notifDTO.TimeStamp</h3>
        </div>
    }
*@

<div class="w-full flex-col">
    <label> Title@* notifDTO.Title *@</label>
    <p> Content@* notifDTO.Content *@</p>
    <h3> Time Stamp @* notifDTO.TimeStamp *@</h3>
</div>

@code {
    //[Parameter] NotificationDTO notifDTO { get; set; }
    
    //TODO: Create function so when clicked, Marked as read.
    //TODO: Change div color depending on isRead.
}
